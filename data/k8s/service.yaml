apiVersion: v1
kind: Service
metadata:
  name: gcp-batch-pipeline-data-generator-service
  namespace: default
  labels:
    app: gcp-batch-pipeline-data-generator
spec:
  type: ClusterIP
  selector:
    app: gcp-batch-pipeline-data-generator
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
